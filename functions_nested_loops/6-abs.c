#include "main.h"
/**
 * _abs - function computes the absolute value of a number
 * @a: function parameter
 * Return: n
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}



}
